# TODO

- [x] Fix `frontpage.html` to be a complete valid HTML document and remove duplicated/incorrect nested markup.
- [ ] Re-open in browser to confirm it renders.
- [ ] If `localhost` still shows `ERR_CONNECTION_REFUSED`, start/confirm the correct local server and port.
